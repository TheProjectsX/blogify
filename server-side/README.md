# Server Side codes of Blogify!

### To test the code:

-   Create `.env` file with the data corresponding to `.env.example` file
-   One CMD on the directory and run:

```bash
npm install
node index.js
```
